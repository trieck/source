/////////////////////////////////////////////////////////////////////////////
//
//	DBDLG.H : database dialog
//

#ifndef __DBDLG_H__
#define __DBDLG_H__

/////////////////////////////////////////////////////////////////////////////
typedef struct {
	HWND parent;
	DLGPROC proc;
	HWND hWnd;
} DbDialog;

DbDialog * createDbDialog(void);
BOOL DbDialogDisplay(const DbDialog *);
void deleteDbDialog(DbDialog * plg);
/////////////////////////////////////////////////////////////////////////////

#endif // __DBDLG_H__